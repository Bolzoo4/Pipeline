# Pipeline v4.1 — GPU Requirements
# Tested on RunPod PyTorch 2.4.1 template (Python 3.11, CUDA 12.4)
# ──────────────────────────────────────────────────────────────

# ─── Pipeline core ───
click
Pillow
numpy
opencv-python-headless

# ─── 3D mesh ───
trimesh
pygltflib

# ─── Background removal (rembg) ───
onnxruntime-gpu

# ─── InstantMesh dependencies ───
# (installed separately via InstantMesh/requirements.txt)
# diffusers, transformers, accelerate, einops, omegaconf,
# pytorch-lightning, xatlas, rembg, nvdiffrast, etc.
# See setup_runpod.sh for the full install procedure.
