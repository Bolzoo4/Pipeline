# GPU Requirements for RunPod / Cloud GPU
# Install with: pip install -r requirements-gpu.txt

# PyTorch (CUDA 12.1)
--extra-index-url https://download.pytorch.org/whl/cu121
torch>=2.1.0
torchvision>=0.16.0

# HuggingFace
transformers>=4.40.0
diffusers>=0.28.0
accelerate>=0.28.0
safetensors>=0.4.0

# Image processing
opencv-python-headless>=4.9.0
Pillow>=10.2.0
numpy>=1.26.0

# CLI
click>=8.1.0

# Optional: native SAM2 (if not using HF pipeline)
# pip install git+https://github.com/facebookresearch/sam2.git
